sudo: required
services:
  - docker

before_install:
  - docker build -t atharavjadhav/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true atharavjadhav/docker-react npm run test

language: generic